import React from "react";

export default function Header() {
  return (
    <header className="h-12 bg-primary text-white p-3 sticky mb-6 shadow-thick">
      <nav className="h-full w-min grid grid-flow-row">
        <button>Globaleda</button>
      </nav>
    </header>
  );
}
