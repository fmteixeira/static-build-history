import React from "react";

export default function Header() {
  return (
    <header className="h-12 bg-blue-600 text-white p-3 sticky">
      <nav className="h-full w-min grid grid-flow-row">
        <button>Globaleda</button>
      </nav>
    </header>
  );
}
